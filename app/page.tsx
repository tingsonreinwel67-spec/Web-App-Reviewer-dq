'use client'

import { useState } from 'react'
import { ArrowLeft, ArrowRight, BarChart3, BookOpen, Check, ChevronLeft, ChevronRight, Clock3, Home, RotateCcw, Target } from 'lucide-react'

type Screen = 'dashboard' | 'progress' | 'flashcard' | 'memorize' | 'practice'
type Answer = { id: string; text: string; correct?: boolean }
type Question = { id: number; section: string; prompt: string; answers: Answer[]; explanation: string }

// Keep this content hard-coded or replace it with your own exam content.
const questions: Question[] = [
  { id: 1, section: 'Insurance Basics', prompt: 'Which principle of insurance requires the insured to disclose all material facts to the insurer?', answers: [{ id: 'a', text: 'Indemnity' }, { id: 'b', text: 'Utmost good faith', correct: true }, { id: 'c', text: 'Contribution' }, { id: 'd', text: 'Subrogation' }], explanation: 'Utmost good faith requires both parties to disclose all material facts that could influence the underwriting decision.' },
  { id: 2, section: 'Life & Health', prompt: 'What is the primary purpose of a life insurance beneficiary designation?', answers: [{ id: 'a', text: 'To set the policy premium' }, { id: 'b', text: 'To select the policy owner' }, { id: 'c', text: 'To identify who receives the death benefit', correct: true }, { id: 'd', text: 'To determine the policy term' }], explanation: 'A beneficiary designation names the person or entity who receives the policy proceeds after the insured dies.' },
  { id: 3, section: 'Regulation', prompt: 'An insurance producer must keep client records for how long after a policy is issued?', answers: [{ id: 'a', text: 'One year' }, { id: 'b', text: 'Three years' }, { id: 'c', text: 'Five years', correct: true }, { id: 'd', text: 'Ten years' }], explanation: 'Most state regulations require producers to retain transaction records for at least five years.' },
]

function ProgressBar({ value, color = 'green' }: { value: number; color?: 'green' | 'blue' }) { return <div className="h-2.5 overflow-hidden rounded-full bg-muted"><div className={`h-full rounded-full ${color === 'blue' ? 'bg-primary' : 'bg-emerald-500'}`} style={{ width: `${value}%` }} /></div> }
function Header({ onHome, onProgress }: { onHome: () => void; onProgress: () => void }) { return <header className="flex items-center justify-between px-5 py-5"><button onClick={onHome} className="flex items-center gap-2 font-mono text-sm font-bold tracking-tight text-foreground"><span className="flex size-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><BookOpen className="size-4" /></span> REVIEWER</button><button onClick={onProgress} className="rounded-full p-2 text-muted-foreground hover:bg-muted" aria-label="View progress"><BarChart3 className="size-5" /></button></header> }
function Card({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-3xl border border-border bg-card p-5 shadow-sm ${className}`}>{children}</section> }

function Dashboard({ go }: { go: (screen: Screen) => void }) { return <main className="mx-auto flex w-full max-w-2xl flex-col gap-5 px-5 pb-10"><div className="pt-3"><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Insurance licensing</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-foreground">Your Reviewer</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">Choose a study mode and keep moving toward exam day.</p></div><Card><div className="mb-4 flex items-center justify-between"><h2 className="text-lg font-bold">Your progress</h2><Target className="size-5 text-primary" /></div><div className="grid grid-cols-2 gap-3"><div className="rounded-2xl bg-muted p-4"><p className="font-mono text-xs uppercase text-muted-foreground">VUL</p><p className="mt-2 text-2xl font-bold">68%</p><ProgressBar value={68} /></div><div className="rounded-2xl bg-muted p-4"><p className="font-mono text-xs uppercase text-muted-foreground">Traditional</p><p className="mt-2 text-2xl font-bold">42%</p><ProgressBar value={42} color="blue" /></div></div></Card><Card><div className="flex flex-col gap-3"><button onClick={() => go('flashcard')} className="flex items-center justify-between rounded-2xl border border-border px-4 py-4 text-left font-semibold transition hover:border-primary"><span className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-xl bg-muted"><BookOpen className="size-4" /></span> Flash Card</span><ArrowRight className="size-4 text-muted-foreground" /></button><button onClick={() => go('memorize')} className="flex items-center justify-between rounded-2xl border border-border px-4 py-4 text-left font-semibold transition hover:border-primary"><span className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-xl bg-muted"><RotateCcw className="size-4" /></span> Memorize</span><ArrowRight className="size-4 text-muted-foreground" /></button><button onClick={() => go('practice')} className="flex items-center justify-between rounded-2xl bg-primary px-4 py-4 text-left font-semibold text-primary-foreground transition hover:opacity-90"><span className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-xl bg-primary-foreground/15"><Target className="size-4" /></span> Practice Exam</span><ArrowRight className="size-4" /></button></div></Card><button onClick={() => go('progress')} className="flex items-center justify-center gap-2 py-2 text-sm font-semibold text-primary">View my progress <ArrowRight className="size-4" /></button></main> }

function StudyPage({ mode, go }: { mode: 'flashcard' | 'memorize'; go: (s: Screen) => void }) { const [index, setIndex] = useState(0); const question = questions[index]; const next = () => setIndex((index + 1) % questions.length); return <main className="mx-auto flex w-full max-w-2xl flex-col gap-5 px-5 pb-10"><button onClick={() => go('dashboard')} className="flex items-center gap-1 self-start text-sm text-muted-foreground"><ChevronLeft className="size-4" /> Back</button><div className="flex items-center justify-between"><p className="font-mono text-xs uppercase tracking-[0.18em] text-muted-foreground">{mode === 'flashcard' ? 'Flash Card' : 'Memorize'}</p><span className="text-xs text-muted-foreground">{index + 1} / {questions.length}</span></div><ProgressBar value={((index + 1) / questions.length) * 100} /><Card className="min-h-64"><p className="font-mono text-xs uppercase text-primary">{question.section}</p><h1 className="mt-6 text-xl font-semibold leading-8">{question.prompt}</h1>{mode === 'memorize' && <p className="mt-8 rounded-2xl bg-muted p-4 text-sm leading-6 text-muted-foreground">Think through the answer before revealing it. The correct answer is included in your hard-coded exam content.</p>}</Card><div className="flex items-center justify-between"><button onClick={() => setIndex((index - 1 + questions.length) % questions.length)} className="rounded-xl border border-border p-3" aria-label="Previous"><ChevronLeft className="size-5" /></button><button onClick={next} className="flex items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">Next <ChevronRight className="size-4" /></button></div></main> }

function Practice({ go }: { go: (s: Screen) => void }) { const [index, setIndex] = useState(0); const [selected, setSelected] = useState<string | null>(null); const [submitted, setSubmitted] = useState(false); const question = questions[index]; const next = () => { if (index < questions.length - 1) { setIndex(index + 1); setSelected(null); setSubmitted(false) } else go('progress') }; return <main className="mx-auto flex w-full max-w-2xl flex-col gap-5 px-5 pb-10"><button onClick={() => go('dashboard')} className="flex items-center gap-1 self-start text-sm text-muted-foreground"><ChevronLeft className="size-4" /> Back</button><div className="flex items-center justify-between"><div><p className="font-mono text-xs uppercase tracking-[0.18em] text-muted-foreground">Practice Exam</p><h1 className="mt-2 text-2xl font-bold">Question {index + 1} of {questions.length}</h1></div><Clock3 className="size-5 text-muted-foreground" /></div><ProgressBar value={((index + (submitted ? 1 : 0)) / questions.length) * 100} /><Card><p className="font-mono text-xs uppercase text-primary">{question.section}</p><h2 className="mt-5 text-lg font-semibold leading-7">{question.prompt}</h2><div className="mt-6 flex flex-col gap-3">{question.answers.map((answer) => <button key={answer.id} onClick={() => !submitted && setSelected(answer.id)} className={`rounded-2xl border p-4 text-left text-sm font-medium transition ${selected === answer.id && !submitted ? 'border-primary bg-primary/10' : submitted && answer.correct ? 'border-emerald-500 bg-emerald-50' : submitted && selected === answer.id ? 'border-destructive bg-destructive/10' : 'border-border hover:border-primary'}`}><span className="mr-3 font-mono text-xs uppercase text-muted-foreground">{answer.id}</span>{answer.text}{submitted && answer.correct && <Check className="float-right size-4 text-emerald-600" />}</button>)}</div>{submitted && <p className="mt-5 rounded-2xl bg-muted p-4 text-sm leading-6 text-muted-foreground">{question.explanation}</p>}<button disabled={!selected} onClick={() => submitted ? next() : setSubmitted(true)} className="mt-6 w-full rounded-xl bg-primary px-4 py-3 text-sm font-bold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-40">{submitted ? index === questions.length - 1 ? 'Finish exam' : 'Next question' : 'Check answer'}</button></Card></main> }

function Progress({ go }: { go: (s: Screen) => void }) { return <main className="mx-auto flex w-full max-w-2xl flex-col gap-5 px-5 pb-10"><button onClick={() => go('dashboard')} className="flex items-center gap-1 self-start text-sm text-muted-foreground"><ChevronLeft className="size-4" /> Back</button><div><p className="font-mono text-xs uppercase tracking-[0.18em] text-muted-foreground">My Progress</p><h1 className="mt-2 text-3xl font-bold">Keep going.</h1></div><Card><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">Overall progress</p><p className="mt-1 text-4xl font-bold">55%</p></div><BarChart3 className="size-7 text-primary" /></div><div className="mt-5"><ProgressBar value={55} /></div></Card><Card><h2 className="font-bold">Topic progress</h2><div className="mt-5 flex flex-col gap-5"><div><div className="mb-2 flex justify-between text-sm"><span>VUL</span><strong>68%</strong></div><ProgressBar value={68} /></div><div><div className="mb-2 flex justify-between text-sm"><span>Traditional</span><strong>42%</strong></div><ProgressBar value={42} color="blue" /></div></div></Card></main> }

export default function Page() { const [screen, setScreen] = useState<Screen>('dashboard'); const go = (next: Screen) => setScreen(next); return <div className="min-h-screen bg-background text-foreground"><Header onHome={() => go('dashboard')} onProgress={() => go('progress')} />{screen === 'dashboard' && <Dashboard go={go} />}{screen === 'progress' && <Progress go={go} />}{(screen === 'flashcard' || screen === 'memorize') && <StudyPage mode={screen} go={go} />}{screen === 'practice' && <Practice go={go} />}<footer className="mx-auto flex max-w-2xl justify-center px-5 pb-8"><button onClick={() => go('dashboard')} className="flex items-center gap-2 text-xs text-muted-foreground"><Home className="size-3.5" /> Reviewer home</button></footer></div> }
